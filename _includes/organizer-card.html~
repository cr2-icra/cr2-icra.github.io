<style>
.organizers-grid {
  display: grid;
  gap: 1rem;
  margin-top: 1.5rem;
}

/* Desktop: 5 per row */
@media (min-width: 900px) {
  .organizers-grid {
    grid-template-columns: repeat(5, 1fr);
  }
}

/* Tablet */
@media (min-width: 600px) and (max-width: 899px) {
  .organizers-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* Mobile: 3 per row */
@media (max-width: 599px) {
  .organizers-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.organizer-card {
  text-align: center;
  font-size: 0.85rem;
}

.organizer-card img {
  width: 65px;
  height: 65px;
  border-radius: 50%;
  object-fit: cover;
  margin-bottom: 0.4rem;
}

.organizer-card .name {
  font-weight: 600;
  font-size: 0.9rem;
  margin-bottom: 0.15rem;
}

.organizer-card .affiliation {
  font-size: 0.75rem;
  color: #666;
  line-height: 1.2;
}
</style>

<div class="organizer-card">
  <img src="{{ include.img }}" alt="{{ include.name }}">
  <div class="name">{{ include.name }}</div>
  <div class="affiliation">{{ include.affiliation }}</div>
</div>
